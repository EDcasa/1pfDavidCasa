import { HeadStyleDirective } from './head-style.directive';

describe('HeadStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadStyleDirective();
    expect(directive).toBeTruthy();
  });
});
