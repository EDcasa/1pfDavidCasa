import { DisplayamePipe } from './displayame.pipe';

describe('DisplayamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayamePipe();
    expect(pipe).toBeTruthy();
  });
});
